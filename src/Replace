//Shall replace function names, variables names, and strings with a generalized form of each

public class Replace {

    //Will take name of a function and replace that name with a generic one inside of the new temporary file
    //Store names in a map so that it's easier to replace further in the file?
    public static void replaceFunction () {

    }

    //Will take name of a variable and replace that name with a generic one inside of the new temporary file
    //Store names in a map so that it's easier to replace further in the file?
    public static void replaceVariable () {

    }

    //Will take String and replace that name with a generic one inside of the new temporary file
    public static void replaceString () {

    }
}